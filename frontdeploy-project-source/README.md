# Frontdeploy Extension

A Chrome Extension for scanning X (Twitter) timelines and identifying trending narratives.

## Build Instructions
```bash
npm install
npm run build
```
Load the `apps/extension/build/chrome-mv3-prod` folder in `chrome://extensions`.
